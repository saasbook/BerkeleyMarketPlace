<div class="container">
    <div class="row">
        <div class="col-1"></div>
        <div class="col-5">
            <h4 class="my-4">Statistics</h1>
            <p>Total registered users: <%= User.count %> </p>
            <p>Total posts: <%= Post.count %> </p>
            <p>Total valid posts: <%= Post.get_all_valid_posts.count %> </p>
            <p>Items: <%= Post.get_valid_post(:item).count %> </p>
            <p>Events: <%= Post.get_valid_post(:event).count %> </p>
            <p>Jobs: <%= Post.get_valid_post(:job).count %> </p>
        </div>
        <div class="col-5">
            <h4 class="my-4">Some Plots</h1>
        </div>
        
    </div>
</div>