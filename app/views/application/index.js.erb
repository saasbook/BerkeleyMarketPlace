<% grid = escape_javascript(
  render(
    :partial => 'application/posts_grid',
    :locals => { :posts => @posts }
  )
) %>
$("#results").html("<%= grid %>");

<% page = escape_javascript(paginate @posts) %>
$("#paging").html("<%= page %>");