<% js = escape_javascript(
  render(
    :partial => 'layouts/posts_grid',
    :locals => { :posts => @posts }
  )
) %>
$("#results").html("<%= js %>");

<% page = escape_javascript(paginate @posts) %>
$("#paging").html("<%= page %>");